# TODO

- [ ] Update `contact.html` to add a “Send us a message” form (name, email, message type, full message, optional phone)
- [ ] Add client-side validation on the contact form submission
- [ ] On valid submit, generate a `mailto:` link to `custserv@woolworths.co.za` with subject + body from the form fields
- [ ] Add minimal CSS in `style.css` for form controls and submit button
- [ ] Smoke-test `contact.html` in browser to ensure Leaflet store locator still functions

